apiVersion: v1
kind: Service
metadata:
  name: suite-clinica-service
spec:
  # Using LoadBalancer for external access (as described in infrastructure report)
  type: LoadBalancer
  selector:
    app: suite-clinica
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
